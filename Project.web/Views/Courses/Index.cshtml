@model IEnumerable<SchoolActivityApp.Application.Dtos.Course.CreateCourseDto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
           
            <th>
                @Html.DisplayNameFor(model => model.NombreCursoAula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Capacidad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            
            <td>
                @Html.DisplayFor(modelItem => item.NombreCursoAula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Capacidad)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CursoID }) |
                @Html.ActionLink("Details", "Details", new { id=item.CursoID  }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CursoID })
            </td>
        </tr>
}
    </tbody>
</table>
